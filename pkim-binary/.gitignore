.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc

# pkim runtime artefacts — per-invocation run manifests + dry-run proposals.
# Per-call state lives here; git history tracks the verb behaviour instead.
/runs/
